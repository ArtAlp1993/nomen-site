// Единый мотион-язык NOMEN — один источник easing/длительностей/пружин для всех
// поверхностей (лендинг, персонаж-продукт). Раньше easing [0.22,1,0.36,1]
// дублировался вручную в Reveal/KineticHeadline/TeaserReveal/ChatWidget/
// CryptoCheckout — теперь берётся отсюда. Меняешь темп бренда здесь — меняется
// везде. Совпадает с ощущением видео (пружинные появления, долгое замедление).

// Премиальное замедление: быстрый старт, длинный «выдох». Для framer-motion
// (массив) и для CSS transition (строка) — одно и то же значение.
export const EASE = [0.22, 1, 0.36, 1];
export const EASE_CSS = "cubic-bezier(0.22, 1, 0.36, 1)";

// Длительности (с) — общая шкала темпа.
export const DUR = {
  fast: 0.35, // микро-переходы (ховер, вспышка)
  base: 0.6, // смена состояния (кадр персонажа, панель)
  slow: 0.95, // reveal секций
};

// Пружины для framer-motion — «живые» появления как в видео.
export const SPRING = {
  soft: { type: "spring", stiffness: 170, damping: 22 },
  pop: { type: "spring", stiffness: 260, damping: 18 },
};
